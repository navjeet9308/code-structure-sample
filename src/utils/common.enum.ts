export enum PermissionType {
    BGV,
    DRUG_TEST
}

export enum WorkerType {
    WT_1099,
    WT_W2
}

export enum WorkLocation {
    Remote, Onsite
}

export enum EmploymentType {
    PartTime, FullTime
}

export enum PayoutType {
    Hourly, Weekly, Monthly
}

export enum TitleMatchType {
    MUST_HAVE, NICE_TO_HAVE,DO_NOT_SHOW
}
export enum CandidateNotificationType {
 JOB_OFFER, MATCHING_JOB_AVAILABLE, JOB_APPLY, JOB_STAGE_CHANGE, JOB_SAVE
}
export enum EmailFrequencyType {
    DAILY,WEEKLY
}
export enum SkillLevelTypeVOSchema {
    Expertise, Medium, Fresher
}