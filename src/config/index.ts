import { config } from 'dotenv';
config({ path: `.env` });

export const { NODE_ENV,
    APP_PORT,
    MONGO_URI,
    DB_HOST,
    DB_PORT,
    DB_DATABASE,
    DB_USER,
    DB_PASSWORD,
    LOG_DIR,
    SMTP_SERVER,
    SMTP_PORT,
    SMTP_USER,
    SMTP_PASS,
    SMTP_SECURE,
    AUTH0_CERT,
    AUTH0_SECRET,
    AUTH0_URL,
    AUTH0_CLIENT_ID,
    AUTH0_CLIENT_SECRET,
    AUTH0_AUDIENCE,
    AUTH0_TOKEN,
    CLIENT_DOMAIN,
    ELASTIC_NODE,
    ELASTIC_USERNAME,
    ELASTIC_PASSSWORD,
    ELASTIC_INDEXNAME,
    SQS_QUEUEURL,
    SQS_REGION,
    SQS_ACCESSKEY,
    SQS_SECRETKEY,
    AWS_REGION,
    AWS_ACCESS_KEY_ID,
    AWS_SECRET_ACCESS_KEY,
    AWS_RESUME_S3_BUCKET,
    AWS_CANDIDATE_S3_BUCKET,
    AWS_RESUME_RESPONSE_SQS,
    AWS_SERVICEO_TO_SF_QUEUE_URL,
    SFDC_USER,
    SFDC_URL,
    SFDC_REST_API_USERNAME,
    SFDC_REST_API_GRANT_TYPE,
    SFDC_REST_API_URL,
    SFDC_REST_API_AUTH_URL,
    SFDC_REST_API_PASSWORD,
    SFDC_PASS,
    SFDC_REST_API_CLIENT_ID,
    SFDC_REST_API_CLIENT_SECRET,
    APPLICATION_RECORD_TYPE,
    META_DATA_BASE_URL,
    JOB_BOARD_ID,
    JOB_BOARD_NAME,
    ENABLED_SQS_CONSUMER,
    AWS_TALENT_RESUME_TO_SERVICEO_SQS_URL,
    AWS_CANDIDATE_S3_BUCKET_BASE_URL,
    CANDIDATE_TYPE_ID
} = process.env;